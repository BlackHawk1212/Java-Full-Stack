Before we start our Maven Project

1. We need to install Maven to our PC and Setup environmental variables
2. In order to check the version 
	Open cmd and type 'mvn -version'
3. In IDE (Eclipse in my case) right click on project and select show in terminal
4. type "mvn install" and run
